name: Oracle Instance Creator
on:
  schedule:
    - cron: '*/5 * * * *'
  workflow_dispatch:
  repository_dispatch:
    types: [run]
permissions:
  contents: write
jobs:
  keep-alive:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v3
      - name: Keep Repo Active
        run: |
          echo "Last run: $(date)" > keep_alive.txt
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add keep_alive.txt
          git commit -m "⏰ keep alive: $(date '+%Y-%m-%d %H:%M')" || echo "No changes"
          git push

  try-ad-1:
    runs-on: ubuntu-latest
    steps:
      - name: Install OCI CLI
        run: pip install oci-cli
      - name: Setup OCI Config
        run: |
          mkdir -p ~/.oci
          printf '%s' '${{ secrets.OCI_CONFIG }}' > ~/.oci/config
          printf '%s' '${{ secrets.OCI_KEY }}' > ~/.oci/key.pem
          chmod 600 ~/.oci/key.pem ~/.oci/config
          echo "${{ secrets.SSH_PUB_KEY }}" > ~/.oci/ssh_key.pub
      - name: Try AD-1
        run: |
          set +e
          RESULT=$(oci compute instance launch \
            --availability-domain "ZwpG:EU-FRANKFURT-1-AD-1" \
            --compartment-id "${{ secrets.COMPARTMENT_ID }}" \
            --shape "VM.Standard.A1.Flex" \
            --shape-config '{"ocpus":4,"memoryInGBs":24}' \
            --subnet-id "${{ secrets.SUBNET_ID }}" \
            --image-id "${{ secrets.IMAGE_ID }}" \
            --ssh-authorized-keys-file ~/.oci/ssh_key.pub \
            --assign-public-ip true 2>&1)
          if echo "$RESULT" | grep -q '"lifecycle-state"'; then
            echo "✅ نجح على AD-1!"
            curl -s "https://api.telegram.org/bot${{ secrets.BOT_TOKEN }}/sendMessage" \
                 --data-urlencode "chat_id=${{ secrets.CHAT_ID }}" \
                 --data-urlencode "text=✅ تم إنشاء Oracle Instance على AD-1 بنجاح!"
          else
            echo "❌ AD-1 ممتلئ"
          fi
          exit 0

  try-ad-2:
    runs-on: ubuntu-latest
    steps:
      - name: Install OCI CLI
        run: pip install oci-cli
      - name: Setup OCI Config
        run: |
          mkdir -p ~/.oci
          printf '%s' '${{ secrets.OCI_CONFIG }}' > ~/.oci/config
          printf '%s' '${{ secrets.OCI_KEY }}' > ~/.oci/key.pem
          chmod 600 ~/.oci/key.pem ~/.oci/config
          echo "${{ secrets.SSH_PUB_KEY }}" > ~/.oci/ssh_key.pub
      - name: Try AD-2
        run: |
          set +e
          RESULT=$(oci compute instance launch \
            --availability-domain "ZwpG:EU-FRANKFURT-1-AD-2" \
            --compartment-id "${{ secrets.COMPARTMENT_ID }}" \
            --shape "VM.Standard.A1.Flex" \
            --shape-config '{"ocpus":4,"memoryInGBs":24}' \
            --subnet-id "${{ secrets.SUBNET_ID }}" \
            --image-id "${{ secrets.IMAGE_ID }}" \
            --ssh-authorized-keys-file ~/.oci/ssh_key.pub \
            --assign-public-ip true 2>&1)
          if echo "$RESULT" | grep -q '"lifecycle-state"'; then
            echo "✅ نجح على AD-2!"
            curl -s "https://api.telegram.org/bot${{ secrets.BOT_TOKEN }}/sendMessage" \
                 --data-urlencode "chat_id=${{ secrets.CHAT_ID }}" \
                 --data-urlencode "text=✅ تم إنشاء Oracle Instance على AD-2 بنجاح!"
          else
            echo "❌ AD-2 ممتلئ"
          fi
          exit 0

  try-ad-3:
    runs-on: ubuntu-latest
    steps:
      - name: Install OCI CLI
        run: pip install oci-cli
      - name: Setup OCI Config
        run: |
          mkdir -p ~/.oci
          printf '%s' '${{ secrets.OCI_CONFIG }}' > ~/.oci/config
          printf '%s' '${{ secrets.OCI_KEY }}' > ~/.oci/key.pem
          chmod 600 ~/.oci/key.pem ~/.oci/config
          echo "${{ secrets.SSH_PUB_KEY }}" > ~/.oci/ssh_key.pub
      - name: Try AD-3
        run: |
          set +e
          RESULT=$(oci compute instance launch \
            --availability-domain "ZwpG:EU-FRANKFURT-1-AD-3" \
            --compartment-id "${{ secrets.COMPARTMENT_ID }}" \
            --shape "VM.Standard.A1.Flex" \
            --shape-config '{"ocpus":4,"memoryInGBs":24}' \
            --subnet-id "${{ secrets.SUBNET_ID }}" \
            --image-id "${{ secrets.IMAGE_ID }}" \
            --ssh-authorized-keys-file ~/.oci/ssh_key.pub \
            --assign-public-ip true 2>&1)
          if echo "$RESULT" | grep -q '"lifecycle-state"'; then
            echo "✅ نجح على AD-3!"
            curl -s "https://api.telegram.org/bot${{ secrets.BOT_TOKEN }}/sendMessage" \
                 --data-urlencode "chat_id=${{ secrets.CHAT_ID }}" \
                 --data-urlencode "text=✅ تم إنشاء Oracle Instance على AD-3 بنجاح!"
          else
            echo "❌ AD-3 ممتلئ"
          fi
          exit 0
